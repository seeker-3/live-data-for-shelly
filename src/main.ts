const PAST_TO_CSV = ''

const div = document.createElement('div')

setInterval(async () => {
  // const response = await fetch(PAST_TO_CSV)
  // const data = await response.text()
  // console.log(d3)
  // div.innerText = data
}, 1_000)

export {}
